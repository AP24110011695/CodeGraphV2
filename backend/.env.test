# Used only when explicitly loaded by a developer or CI.
# Create the database and enable the pgvector extension before running integration tests.
TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/codegraph_test
REDIS_URL=redis://localhost:6379/15
SECRET_KEY=test-secret
LLM_API_KEY=test-key
